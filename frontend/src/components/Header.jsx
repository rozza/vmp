import React from 'react';
import styled from 'styled-components';

const HeaderDiv = styled.div`
  font-size: 24px;
`;

const Header = () => (
  <HeaderDiv>Visual MongoDB Pro(totype)!</HeaderDiv>
);

export default Header;
